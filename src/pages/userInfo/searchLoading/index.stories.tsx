import React from "react";
import SearchLoading from "./index";

export default {
  title: "SearchLoading",
  component: SearchLoading,
};

const Template = args => <SearchLoading {...args} />;

export const Default = Template.bind({});

Default.args = {};
